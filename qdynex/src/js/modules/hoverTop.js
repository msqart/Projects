module.exports = (topBlock) => {
    // const bgImg = topBlock.querySelector('.top_section_bg');
    // const width = topBlock.clientWidth;
    // const height = topBlock.clientHeight;
    //
    // function moveImg(even) {
    //     bgImg.style.transform = `translate(${(even.clientX * 100 / width) - 50}%,${(even.clientY * 100 / height) - 50}%)`;
    // }
    //
    // topBlock.addEventListener('mousemove', (even) => {
    //     moveImg(even);
    //
    // })
};